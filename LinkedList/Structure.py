# Structure of a Node (Singly Linked List):
# In Python, you can create a node using a class:
    
class Node:
    def __init__(self, data):
        self.data = data  # stores the value
        self.next = None  # pointer to the next node